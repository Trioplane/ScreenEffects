raw f"# You're probably wondering why this exists."
raw f"# Well because sometimes people like to not follow instructions"
raw f"# and break other's people packs because they probably used"
raw f"# 'set' instead of 'merge' so that's why this 1 line of macro"
raw f"# exists lol."
$data modify storage scrfx:registry screen_effects merge from storage $(storage) $(path)